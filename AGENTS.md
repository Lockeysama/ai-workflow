# 项目约定

- 不自动 commit 或 push。等待用户确认 review 完成并明确指示后再提交。
- 技能源放在 `skills/<name>/`，保持资源路径相对于技能目录；避免写入开发者机器的绝对路径。
- 改技能时维护其 `SKILL.md`、引用文档和需要的回归测试；不因一次示例修改全局表达规则。
- HIL 固定阅读控件，结构错误应修复；内容或布局提示不阻止生成，不为消除提示删减必要信息。
- 改动后运行 `python3 scripts/check.py`。涉及 CSS、目录、图表或打印时增加实际浏览器检查，明确未验证部分。
- 中间文件放 `work/`，生成产物放 `outputs/`，两者均不纳入版本管理。
- 不自动修改个人技能安装副本或外部工具配置。用户要求安装时使用仓库安装脚本。
